<%- include('./partials/header', {title: 'Payment Method', showBackButton: true}) %>

<div class="content-wrapper">
    <div class="grid-layout-full">
        <div class="section-card">
            <div class="section-header">
                <h2>Select Payment Method</h2>
            </div>
            <div class="section-content">
                <div class="payment-methods">
                    <form id="paymentMethodForm" action="/products/process-payment" method="POST">
                        
                        <div class="payment-method-option">
                            <input type="radio" id="airtelMoney" name="paymentMethod" value="AirtelMoney" checked>
                            <label for="airtelMoney" class="payment-method-label">
                                <div class="method-header">
                                    <span class="method-icon">📱</span>
                                    <h3>Airtel Money</h3>
                                </div>
                                <p class="method-description">Pay directly using your Airtel Money account. Fast, secure, and convenient.</p>
                                <div class="method-features">
                                    <ul>
                                        <li>✓ Instant payment confirmation</li>
                                        <li>✓ Available on all Airtel networks</li>
                                        <li>✓ No hidden charges</li>
                                    </ul>
                                </div>
                            </label>
                        </div>

                        <div class="payment-method-option">
                            <input type="radio" id="creditCard" name="paymentMethod" value="CreditCard" disabled>
                            <label for="creditCard" class="payment-method-label disabled">
                                <div class="method-header">
                                    <span class="method-icon">💳</span>
                                    <h3>Credit/Debit Card</h3>
                                </div>
                                <p class="method-description">Coming soon - Pay using your credit or debit card</p>
                                <span class="badge-coming-soon">Coming Soon</span>
                            </label>
                        </div>

                        <div class="payment-method-option">
                            <input type="radio" id="bankTransfer" name="paymentMethod" value="BankTransfer" disabled>
                            <label for="bankTransfer" class="payment-method-label disabled">
                                <div class="method-header">
                                    <span class="method-icon">🏦</span>
                                    <h3>Bank Transfer</h3>
                                </div>
                                <p class="method-description">Coming soon - Transfer funds directly from your bank account</p>
                                <span class="badge-coming-soon">Coming Soon</span>
                            </label>
                        </div>

                        <div class="order-summary mt-4">
                            <h3>Order Summary</h3>
                            <div class="summary-items">
                                <% cart.forEach(item => { %>
                                    <div class="summary-item">
                                        <span><%= item.name %></span>
                                        <span>x<%= item.quantity %></span>
                                        <span class="item-price">K<%= (item.price * item.quantity).toFixed(2) %></span>
                                    </div>
                                <% }); %>
                            </div>
                            <div class="summary-total">
                                <h4>Total Amount: <span class="total-amount">K<%= cart.reduce((total, item) => total + (item.price * item.quantity), 0).toFixed(2) %></span></h4>
                            </div>
                        </div>

                        <button type="submit" class="btn btn-primary btn-lg btn-block mt-4">Continue to Payment</button>
                        <a href="/products/checkout" class="btn btn-secondary btn-block mt-2">Back to Checkout</a>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    .payment-methods {
        padding: 20px 0;
    }

    .payment-method-option {
        margin-bottom: 20px;
    }

    .payment-method-option input[type="radio"] {
        display: none;
    }

    .payment-method-label {
        display: block;
        padding: 20px;
        border: 2px solid #ddd;
        border-radius: 8px;
        cursor: pointer;
        transition: all 0.3s ease;
        background-color: #fff;
    }

    .payment-method-option input[type="radio"]:checked + .payment-method-label {
        border-color: #007bff;
        background-color: #f0f7ff;
        box-shadow: 0 0 10px rgba(0, 123, 255, 0.1);
    }

    .payment-method-label.disabled {
        opacity: 0.6;
        cursor: not-allowed;
        background-color: #f5f5f5;
    }

    .method-header {
        display: flex;
        align-items: center;
        margin-bottom: 15px;
    }

    .method-icon {
        font-size: 40px;
        margin-right: 15px;
    }

    .method-header h3 {
        margin: 0;
        font-size: 20px;
        color: #333;
    }

    .method-description {
        margin: 10px 0;
        color: #666;
        font-size: 15px;
    }

    .method-features ul {
        list-style: none;
        padding: 0;
        margin: 10px 0 0 0;
    }

    .method-features li {
        padding: 5px 0;
        color: #555;
        font-size: 14px;
    }

    .badge-coming-soon {
        display: inline-block;
        background-color: #e9ecef;
        color: #6c757d;
        padding: 5px 10px;
        border-radius: 4px;
        font-size: 12px;
        font-weight: bold;
        margin-top: 10px;
    }

    .order-summary {
        background-color: #f8f9fa;
        padding: 20px;
        border-radius: 8px;
        border: 1px solid #dee2e6;
    }

    .order-summary h3 {
        margin-top: 0;
        margin-bottom: 15px;
        color: #333;
    }

    .summary-items {
        margin-bottom: 15px;
    }

    .summary-item {
        display: flex;
        justify-content: space-between;
        padding: 10px 0;
        border-bottom: 1px solid #dee2e6;
        font-size: 15px;
    }

    .summary-item:last-child {
        border-bottom: none;
    }

    .item-price {
        font-weight: bold;
        color: #007bff;
    }

    .summary-total {
        padding-top: 15px;
        border-top: 2px solid #dee2e6;
        text-align: right;
    }

    .summary-total h4 {
        margin: 0;
        font-size: 18px;
        color: #333;
    }

    .total-amount {
        color: #28a745;
        font-size: 20px;
    }

    .btn {
        padding: 12px 20px;
        border: none;
        border-radius: 4px;
        font-size: 16px;
        cursor: pointer;
        text-decoration: none;
        display: inline-block;
        transition: all 0.3s ease;
    }

    .btn-primary {
        background-color: #007bff;
        color: white;
        width: 100%;
    }

    .btn-primary:hover {
        background-color: #0056b3;
    }

    .btn-secondary {
        background-color: #6c757d;
        color: white;
        width: 100%;
        text-align: center;
    }

    .btn-secondary:hover {
        background-color: #5a6268;
    }

    .btn-lg {
        padding: 15px 30px;
        font-size: 18px;
    }

    .btn-block {
        display: block;
        width: 100%;
    }

    .mt-2 {
        margin-top: 15px;
    }

    .mt-4 {
        margin-top: 30px;
    }

    @media (max-width: 768px) {
        .payment-method-label {
            padding: 15px;
        }

        .method-header {
            flex-direction: column;
            align-items: flex-start;
        }

        .method-icon {
            margin-right: 0;
            margin-bottom: 10px;
        }

        .summary-item {
            flex-wrap: wrap;
        }

        .item-price {
            width: 100%;
            text-align: right;
        }
    }
</style>

<%- include('./partials/footer') %>
</body>
</html>
